package com.player.Type;

import com.player.Player;
import com.raylib.Vector2;
import com.raylib.Rectangle;

public class Warior extends Player
{
	public Warior(Vector2 position, Vector2 size, Rectangle colisionBox,
		float scale, Vector2 offset)
	{
		super(position, size, colisionBox, scale, offset);
	}
}
